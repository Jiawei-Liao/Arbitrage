import { createTheme } from '@mui/material/'

const theme = createTheme({
    palette: {
        primary: {
            main: '#0056B3'
        },
        secondary: {
            main: '#2ECC71'
        },
        warning: {
            main: '#FFC107'
        }
    }
})

export default theme